warning: in the working copy of 'src/screens/space/brain/generativeCore.ts', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/src/screens/space/brain/generativeCore.ts b/src/screens/space/brain/generativeCore.ts[m
[1mindex 94acc7ec53..19e94497dd 100644[m
[1m--- a/src/screens/space/brain/generativeCore.ts[m
[1m+++ b/src/screens/space/brain/generativeCore.ts[m
[36m@@ -2,33 +2,53 @@[m [mimport type { BrainContext, RankedPost } from './types';[m
 import { getUi } from './locales';[m
 import { softReaction } from './emotionEngine';[m
 import { maybeHumor } from './humorEngine';[m
[31m-import { compactText, pick, sentence } from './text';[m
[32m+[m[32mimport { compactText, hashText, pick, sentence } from './text';[m
 import { describeSignal } from './signalEngine';[m
[32m+[m[32mimport { MARGELET_CORE_PHRASES, resolveManifestLang } from '../knowledge';[m
 [m
 function namePrefix(ctx: BrainContext) {[m
[31m-  if (!ctx.memory.userName || ctx.memory.turns % 5 !== 0) return '';[m
[32m+[m[32m  if (!ctx.memory.userName) return '';[m
[32m+[m[32m  if ((ctx.memory.turns + hashText(ctx.query)) % 6 !== 0) return '';[m
   return `${ctx.memory.userName}, `;[m
 }[m
 [m
[32m+[m[32mfunction cleanTail(ctx: BrainContext, seed: number) {[m
[32m+[m[32m  if (ctx.intent === 'product' || ctx.intent === 'investor') return '';[m
[32m+[m[32m  return softReaction(ctx.mood, seed) + maybeHumor(ctx);[m
[32m+[m[32m}[m
[32m+[m
 export function generateTalk(ctx: BrainContext, variants: string[]) {[m
[31m-  const seed = ctx.query.length + ctx.memory.turns + ctx.rawTokens.join('').length;[m
[31m-  const tail = softReaction(ctx.mood, seed) + maybeHumor(ctx);[m
[31m-  return `${namePrefix(ctx)}${pick(variants, seed)}${tail}`.trim();[m
[32m+[m[32m  const seed = hashText(ctx.query) + ctx.memory.turns + ctx.rawTokens.join('').length;[m
[32m+[m[32m  return `${namePrefix(ctx)}${pick(variants, seed)}${cleanTail(ctx, seed)}`.trim();[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32mexport function generateProductTalk(ctx: BrainContext) {[m
[32m+[m[32m  const phrases = MARGELET_CORE_PHRASES[resolveManifestLang(ctx.lang)];[m
[32m+[m[32m  const seed = hashText(ctx.query) + ctx.memory.turns;[m
[32m+[m[32m  const opener = pick([m
[32m+[m[32m    ctx.intent === 'investor'[m
[32m+[m[32m      ? ['Покажу, почему margeleT — не просто лента, а поисковый слой Telegram.'][m
[32m+[m[32m      : ['margeleT превращает хаос Telegram в понятную карту внимания.'],[m
[32m+[m[32m    seed[m
[32m+[m[32m  );[m
[32m+[m[32m  const phrase = pick(phrases, seed + 3);[m
[32m+[m[32m  if (ctx.lang === 'ru') return `${opener}\n\n${phrase}`;[m
[32m+[m[32m  return `${opener}\n\n${phrase}`;[m
 }[m
 [m
 export function generateNoResult(ctx: BrainContext) {[m
[31m-  const seed = ctx.query.length + ctx.memory.turns;[m
[32m+[m[32m  const seed = hashText(ctx.query) + ctx.memory.turns;[m
   if (ctx.lang === 'ru') {[m
     const variants = [[m
[31m-      'В моём текущем потоке margeleT про это тихо. В интернете наверняка что-то есть — но у себя сейчас не вижу чистого сигнала.',[m
[31m-      'Не хочу притягивать случайный пост. Сейчас в margeleT нет точного совпадения, можно дать ещё одну зацепку — и я сузю поиск.',[m
[31m-      'Пока пусто по моей базе. Значит, в Telegram-потоке margeleT это сейчас не вспыхнуло.',[m
[32m+[m[32m      'Сейчас внутри margeleT об этом почти ничего не говорят. Не буду притягивать случайные посты.',[m
[32m+[m[32m      'У себя не вижу чистого сигнала. Можно дать ещё одну зацепку — страну, канал или другое слово.',[m
[32m+[m[32m      'Пока тихо в моём Telegram-потоке. Это не значит, что темы нет вообще — просто в margeleT она сейчас не вспыхнула.',[m
     ];[m
     return generateTalk(ctx, [pick(variants, seed)]);[m
   }[m
   return generateTalk(ctx, [[m
[31m-    'It is quiet in my current margeleT flow. The wider internet may have it, but I do not see a clean signal here yet.',[m
[31m-    'I do not want to force a random post. Give me one more clue and I’ll narrow it down.',[m
[32m+[m[32m    'It is quiet inside my current margeleT flow. I will not force random posts.',[m
[32m+[m[32m    'I do not see a clean signal here. Add a country, source, or one more word and I’ll narrow it down.',[m
   ]);[m
 }[m
 [m
[36m@@ -36,7 +56,7 @@[m [mexport function generateFound(ctx: BrainContext, ranked: RankedPost[]) {[m
   const best = ranked[0]?.post;[m
   const ui = getUi(ctx.lang);[m
   if (!best) return generateNoResult(ctx);[m
[31m-  const seed = ctx.query.length + Math.round(ranked[0].score) + ctx.memory.turns;[m
[32m+[m[32m  const seed = hashText(ctx.query) + Math.round(ranked[0].score) + ctx.memory.turns;[m
   const signal = describeSignal(ctx.lang, ranked);[m
 [m
   if (!ctx.shouldShowBlocks) {[m
[36m@@ -46,8 +66,8 @@[m [mexport function generateFound(ctx: BrainContext, ranked: RankedPost[]) {[m
       : generateTalk(ctx, [`I see a close thought in the flow: ${piece}`]);[m
   }[m
 [m
[31m-  if (ctx.intent === 'images') return pick([ui.foundGallery, 'Собрал визуальные совпадения. Покажу без простыни.'], seed);[m
[31m-  if (ctx.intent === 'video') return pick([ui.foundVideo, 'Есть близкие видео. Сначала маленькая подборка.'], seed);[m
[32m+[m[32m  if (ctx.intent === 'images') return pick([ui.foundGallery, 'Собрал визуальные совпадения. Без простыни.'], seed);[m
[32m+[m[32m  if (ctx.intent === 'video') return pick([ui.foundVideo, 'Есть близкие видео. Сначала небольшая подборка.'], seed);[m
   if (ctx.intent === 'weather') return pick(['Похоже, есть погодный сигнал из Telegram.', 'Нашёл погодную зацепку в потоке.'], seed);[m
   if (ctx.intent === 'recipe') return pick([ui.recipeFound, 'Нашёл похожий живой рецепт. Сначала один вариант.'], seed);[m
   if (ctx.intent === 'trend') return pick([`Вижу сигнал: ${signal}.`, ui.foundOne], seed);[m
