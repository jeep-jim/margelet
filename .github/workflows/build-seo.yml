name: 🔍 SEO

on:
  workflow_dispatch:
  schedule:
    # Two times per day. UTC time, after rebuild feed snapshots.
    - cron: "30 */3 * * *"

permissions:
  contents: write

concurrency:
  group: build-seo-main
  cancel-in-progress: false

jobs:
  build-seo:
    runs-on: ubuntu-latest
    timeout-minutes: 55

    env:
      NPM_CONFIG_REGISTRY: https://registry.npmjs.org/

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Show workflow version
        run: echo "workflow version = seo-quality-2x-daily-v1"

      - name: Install dependencies
        run: npm ci

      - name: Build SEO snapshot
        run: npm run build:seo

      - name: Commit SEO files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add -A \
            public/country \
            public/seo \
            public/post \
            public/sitemap.xml \
            public/sitemaps

          if git diff --cached --quiet; then
            echo "No SEO changes to commit"
            exit 0
          fi

          git commit -m "rebuild seo"
          git pull --rebase origin main || true
          git push
